version: '1'
services:
  microservice:
    build:
        context: .
        dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - spring.datasource.url=jdbc:oracle:thin:@eeg7gozk9ct7wyk1_medium?TNS_ADMIN=/app/oracle_wallet/
      - spring.datasource.username=HotelGuess_123
      - spring.datasource.password=HotelGuess123#
    restart: always